import 'package:flutter/material.dart';

class SudokuRulesDescription extends StatelessWidget {
  const SudokuRulesDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("sudoku rules");
  }
}